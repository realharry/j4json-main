package j4json.builder;


/**
 * Base JSON string builder interface
 * (which uses only Map and List).
 * This is the primary base interface for MiniJson JSON builder implementations.
 */
public interface BareJsonBuilder extends IndentedJsonBuilder, JsonStructureBuilder
{
}
